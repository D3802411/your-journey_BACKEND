<form method="post" action="{{ path('app_article_delete_comment', {'id': comment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete_comm_' ~ comment.id) }}">
    <button class="btn">Delete</button>
</form>